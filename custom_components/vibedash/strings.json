{
  "config": {
    "step": {
      "user": {
        "title": "Set up VibeDash",
        "description": "Choose how VibeDash connects to an LLM. Select 'AI Task' to use a Home Assistant integration, or pick a provider for direct API access with streaming.",
        "data": {
          "streaming_provider": "Provider",
          "ai_task_entry": "AI Task Entity",
          "streaming_api_key": "API Key",
          "streaming_model": "Model",
          "streaming_base_url": "API Base URL"
        }
      }
    },
    "abort": {
      "already_configured": "VibeDash is already configured."
    },
    "error": {
      "streaming_validation_failed": "Could not connect to provider: {error_detail}"
    }
  }
}
